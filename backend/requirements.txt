fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
sqlalchemy==2.0.35
alembic==1.13.3
psycopg2-binary==2.9.9
asyncpg==0.29.0
python-multipart==0.0.9
nats-py==2.7.2
httpx==0.27.2
reportlab==4.2.5
minio==7.2.9
pgvector==0.3.4
numpy==1.26.4
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
tenacity==9.0.0
python-dotenv==1.0.1
pybatfish==2024.7.22.1569
pandas==2.2.3
pytest==8.3.3
pytest-asyncio==0.24.0
respx==0.21.1

# Phase 1 trained-AI pipeline (DistilBERT classifier + all-MiniLM-L6-v2
# embeddings). Optional at runtime: app/ai/classifier.py and
# app/ai/embeddings.py detect their absence and fall back to deterministic
# heuristics so the API still boots/tests without these installed.
transformers==4.44.2
sentence-transformers==3.1.1
torch==2.4.1
python-dotenv

# Cloud Collection API SDKs (Phase 8 Extension)
boto3==1.35.15
azure-mgmt-network==25.0.0
azure-identity==1.17.1
azure-storage-blob==12.22.0
paramiko==3.5.0
google-cloud-compute==1.19.1
google-auth==2.34.0
pywebpush==2.0.1

# Device transport libraries — collectors/snmp.py, netconf.py, gnmi.py all
# soft-import these (module boots fine without them, collector calls just
# return "<lib> is not installed" errors). Previously missing here, which
# meant every SNMP/NETCONF/gNMI collection silently failed even though the
# collector, gateway, and frontend wiring were all otherwise correct.
pysnmp==7.1.16
ncclient==0.6.15
pygnmi==0.8.13

# Unified Control Library — document_ingestion_service.py soft-imports
# these (module boots and falls back to regex tag-stripping without them,
# same pattern as the SNMP/NETCONF/gNMI libs above).
pdfplumber==0.11.4
beautifulsoup4==4.12.3